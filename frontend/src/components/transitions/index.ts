/**
 * View transition primitives for the Enterprise FizzBuzz Platform.
 *
 * PageTransition orchestrates crossfade animations between dashboard pages
 * using the View Transitions API with CSS fallback. SharedElement marks
 * elements for cross-page morphing continuity.
 */

export { PageTransition } from "./page-transition";
export { SharedElement } from "./shared-element";
